# Multiclass Classification Chapter

Canonical tutorial code for a PyTorch multiclass classifier on synthetic points.

```powershell
uv run pytest chapters/fundamentals/multiclass_classification/tests -q
```
