# DDIM Chapter

Canonical DDIM tutorial code lives here. Checkpoints and generated images go to
`work_dirs/ddim/`.

```powershell
uv run python -m chapters.generative_models.ddim.main
uv run pytest chapters/generative_models/ddim/tests -q
```
