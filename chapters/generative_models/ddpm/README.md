# DDPM Chapter

Canonical DDPM tutorial code lives here. Checkpoints and generated images go to
`work_dirs/ddpm/`.

```powershell
uv run python -m chapters.generative_models.ddpm.ddpm
uv run pytest chapters/generative_models/ddpm/tests -q
```
