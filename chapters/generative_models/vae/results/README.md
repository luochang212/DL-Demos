# VAE Results

Commit only small summaries here, such as `metrics.json`, short CSV curves, or
small sample grids used by the tutorial. Large checkpoints and full training
outputs should stay in `work_dirs/vae/` or external storage.
