# PixelCNN Chapter

Canonical PixelCNN tutorial code lives here. Checkpoints and generated images go
to `work_dirs/pixelcnn/`.

```powershell
uv run pytest chapters/generative_models/pixelcnn/tests -q
```
