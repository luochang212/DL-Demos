# Normalize text files to LF in the repository and working tree for
# consistent behavior across Windows, macOS, and Linux.
* text=auto eol=lf

# Windows-only scripts keep CRLF.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
