---
id: rnn
title: RNN 鈥?寰幆绁炵粡缃戠粶
sidebar_label: 1. RNN 鈥?寰幆绁炵粡缃戠粶
description: 鐞嗚В RNN 鐨勫惊鐜粨鏋勫拰娌挎椂闂存灞曞紑鐨勮绠楁柟寮忥紝鎺屾彙瀛楃绾ц瑷€妯″瀷鐨勮缁冧笌閲囨牱锛屽苟閫愯瑙ｆ瀽 PyTorch 瀹炵幇銆?
keywords: [RNN, GRU, 寰幆绁炵粡缃戠粶, 璇█妯″瀷, 瀛楃绾? 搴忓垪寤烘ā, PyTorch]
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# RNN 鈥?寰幆绁炵粡缃戠粶

:::tip 鏈珷瑕佺偣
- 鐞嗚В **RNN 鐨勫惊鐜粨鏋?*锛氶殣钘忕姸鎬佸浣曞湪鏃堕棿姝ヤ箣闂翠紶閫?
- 鎺屾彙 **瀛楃绾ц瑷€妯″瀷**锛氶€愬瓧绗﹂娴嬩笅涓€涓瓧绗?
- 瀵规瘮**鎵嬪啓 RNN** 涓?**nn.GRU** 鐨勪袱绉嶅疄鐜?
- 璇绘噦 [`chapters/sequence_models/basic_rnn/models.py`](https://github.com/luochang212/DL-Demos/blob/master/chapters/sequence_models/basic_rnn/models.py) 鐨勪袱绉?RNN 瀹炵幇
:::

---

## 1. 涓轰粈涔堥渶瑕?RNN

鍏ㄨ繛鎺ョ綉缁滃拰 CNN 鍋囪杈撳叆涔嬮棿鐩镐簰鐙珛銆備絾寰堝鏁版嵁澶╃劧鏄?*搴忓垪**锛?

- 鏂囨湰锛氥€屾垜浠婂ぉ銆嶁啋 涓嬩竴涓瓧鏄€屽幓銆嶆瘮銆屼簡銆嶆洿鍚堢悊
- 鏃堕棿搴忓垪锛氳偂绁ㄤ环鏍笺€佸ぉ姘斿彉鍖?
- 璇煶锛氬墠鍚庡抚寮虹浉鍏?

RNN 閫氳繃**闅愯棌鐘舵€?*锛坔idden state锛夊湪鏃堕棿姝ヤ箣闂翠紶閫掍俊鎭紝璁╃綉缁滃叿鏈夈€岃蹇嗐€嶃€?

---

## 2. 鏁板鍘熺悊

### 2.1 鍩烘湰缁撴瀯

RNN 鍦ㄦ瘡涓椂闂存 $t$ 鐨勮绠楋細

$$
a^{(t)} = g(W_a [a^{(t-1)}, x^{(t)}] + b_a)
$$

$$
\hat{y}^{(t)} = \sigma(W_y a^{(t)} + b_y)
$$

鍏朵腑 $a^{(t)}$ 鏄殣钘忕姸鎬侊紝$[\cdot, \cdot]$ 琛ㄧず鎷兼帴锛?g$ 閫氬父鏄?$\tanh$銆?

```
鏃堕棿姝ュ睍寮€锛?

x(1) 鈹€鈹€鈫?[RNN] 鈹€鈹€鈫?x(2) 鈹€鈹€鈫?[RNN] 鈹€鈹€鈫?x(3) 鈹€鈹€鈫?...
            鈹?                  鈹?                  鈹?
            a(1) 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈫?  a(2) 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈫?  a(3)
            鈹?                  鈹?                  鈹?
           欧(1)               欧(2)               欧(3)
```

姣忎釜鏃堕棿姝ュ叡浜悓涓€缁勬潈閲?$W_a, W_y$銆?

### 2.2 瀛楃绾ц瑷€妯″瀷

灏嗗崟璇嶇殑姣忎釜瀛楃鐢?one-hot 缂栫爜杈撳叆 RNN锛?

- **杈撳叆**锛氬綋鍓嶅瓧绗︾殑 embedding锛堟垨 one-hot锛?
- **鐩爣**锛氶娴嬩笅涓€涓瓧绗︾殑姒傜巼鍒嗗竷
- **璁粌**锛氭渶灏忓寲浜ゅ弶鐔?$\sum_t -\log P(c^{(t+1)} \mid c^{(1)}, ..., c^{(t)})$

璁粌瀹屾垚鍚庯紝RNN 鍙互鎸夐娴嬫鐜囧垎甯冮殢鏈洪噰鏍蜂笅涓€涓瓧绗︼紝寰幆鐢熸垚鏂扮殑銆屽崟璇嶃€嶃€?

### 2.3 姊害瑁佸壀

RNN 鐨勬搴﹂€氳繃鏃堕棿姝ュ弽鍚戜紶鎾紙BPTT锛夛紝缁忚繃澶氫釜鏃堕棿姝ュ悗瀹规槗姊害鐖嗙偢銆傚父鐢ㄧ殑瑙ｅ喅鏂规鏄?*姊害瑁佸壀**锛?

$$
\text{if } \|\nabla\| > \theta: \quad \nabla \leftarrow \frac{\theta}{\|\nabla\|} \nabla
$$

---

## 3. 浠ｇ爜瑙ｆ瀽

鏈」鐩疄鐜颁簡涓ょ RNN锛?
- **RNN1**锛氭墜鍔ㄥ惊鐜紝one-hot 杈撳叆锛屽睍绀?RNN 鐨勫簳灞傝绠?
- **RNN2**锛氫娇鐢?`nn.GRU` + `nn.Embedding`锛屾洿宸ョ▼鍖栫殑鍐欐硶

涓よ€呴兘鍦?IMDB 璇嶆眹琛ㄤ笂璁粌瀛楃绾ц瑷€妯″瀷銆?

### 3.1 RNN1锛氭墜鍔ㄥ惊鐜疄鐜?

```python title="chapters/sequence_models/basic_rnn/models.py" showLineNumbers
class RNN1(nn.Module):

    def __init__(self, hidden_units=32):
        super().__init__()
        self.hidden_units = hidden_units
        # key-start
        # 涓€涓?Linear 鍚屾椂澶勭悊 [a(t-1), x(t)] 鐨勬嫾鎺?
        self.linear_a = nn.Linear(hidden_units + EMBEDDING_LENGTH, hidden_units)
        self.linear_y = nn.Linear(hidden_units, EMBEDDING_LENGTH)
        self.tanh = nn.Tanh()
        # key-end

    def forward(self, word):
        batch, Tx = word.shape[0:2]
        word = torch.transpose(word, 0, 1)  # (Tx, batch, embedding)
        output = torch.empty_like(word)

        a = torch.zeros(batch, self.hidden_units, device=word.device)
        x = torch.zeros(batch, EMBEDDING_LENGTH, device=word.device)
        # key-start
        for i in range(Tx):
            # 鏍稿績寰幆锛歛(t) = tanh(W 路 [a(t-1), x(t)] + b)
            next_a = self.tanh(self.linear_a(torch.cat((a, x), 1)))
            hat_y = self.linear_y(next_a)   # 棰勬祴涓嬩竴涓瓧绗?
            output[i] = hat_y
            x = word[i]                     # 涓嬩竴涓椂闂存鐨勮緭鍏?
            a = next_a                       # 浼犻€掗殣钘忕姸鎬?
        # key-end
        return torch.transpose(output, 0, 1)
```

### 3.2 璇█妯″瀷锛氳绠楀簭鍒楁鐜?

```python title="chapters/sequence_models/basic_rnn/models.py" showLineNumbers
    @torch.no_grad()
    def language_model(self, word):
        batch, Tx = word.shape[0:2]
        word = torch.transpose(word, 0, 1)
        word_label = torch.argmax(word, 2)  # one-hot 鈫?index

        # key-start
        # P(word) = 鈭?P(c(t+1) | c(1), ..., c(t))
        output = torch.ones(batch, device=word.device)
        a = torch.zeros(batch, self.hidden_units, device=word.device)
        x = torch.zeros(batch, EMBEDDING_LENGTH, device=word.device)
        for i in range(Tx):
            next_a = self.tanh(self.linear_a(torch.cat((a, x), 1)))
            hat_y = F.softmax(self.linear_y(next_a), 1)
            probs = hat_y[torch.arange(batch), word_label[i]]  # 鍙栫洰鏍囧瓧绗︾殑姒傜巼
            output *= probs                                      # 绱箻
            x = word[i]
            a = next_a
        # key-end
        return output
```

### 3.3 閲囨牱锛氱敓鎴愭柊鍗曡瘝

```python title="chapters/sequence_models/basic_rnn/models.py" showLineNumbers
    @torch.no_grad()
    def sample_word(self, device=None):
        if device is None:
            device = next(self.parameters()).device
        output = ''
        a = torch.zeros(1, self.hidden_units, device=device)
        x = torch.zeros(1, EMBEDDING_LENGTH, device=device)
        # key-start
        for i in range(10):
            next_a = self.tanh(self.linear_a(torch.cat((a, x), 1)))
            hat_y = F.softmax(self.linear_y(next_a), 1)
            # 鎸夋鐜囧垎甯冮殢鏈洪噰鏍蜂竴涓瓧绗?
            np_prob = hat_y[0].detach().cpu().numpy()
            letter = np.random.choice(LETTER_LIST, p=np_prob)
            output += letter
            if letter == ' ':  # 绌烘牸 = EOS
                break
            # 閲囨牱鐨勫瓧绗︿綔涓轰笅涓€涓椂闂存鐨勮緭鍏?
            x = torch.zeros(1, EMBEDDING_LENGTH, device=device)
            x[0][LETTER_MAP[letter]] = 1
            a = next_a
        # key-end
        return output
```

### 3.4 RNN2锛欸RU + Embedding 瀹炵幇

```python title="chapters/sequence_models/basic_rnn/models.py" showLineNumbers
class RNN2(nn.Module):

    def __init__(self, hidden_units=64, embeding_dim=64, dropout_rate=0.2):
        super().__init__()
        self.drop = nn.Dropout(dropout_rate)
        # key-start
        self.encoder = nn.Embedding(EMBEDDING_LENGTH, embeding_dim)
        self.rnn = nn.GRU(embeding_dim, hidden_units, 1, batch_first=True)
        self.decoder = nn.Linear(hidden_units, EMBEDDING_LENGTH)
        # key-end

    def forward(self, word):
        batch, Tx = word.shape[0:2]
        first_letter = word.new_zeros(batch, 1)
        x = torch.cat((first_letter, word[:, 0:-1]), 1)  # 寤惰繜涓€姝ワ細鐢ㄥ墠涓€涓瓧绗﹂娴嬪綋鍓?
        hidden = torch.zeros(1, batch, self.hidden_units, device=word.device)
        emb = self.drop(self.encoder(x))
        output, hidden = self.rnn(emb, hidden)
        y = self.decoder(output.reshape(batch * Tx, -1))
        return y.reshape(batch, Tx, -1)
```

**GRU**锛圙ated Recurrent Unit锛夋槸 RNN 鐨勬敼杩涚増鏈紝閫氳繃**鏇存柊闂?*鍜?*閲嶇疆闂?*鎺у埗淇℃伅娴佸姩锛岀紦瑙ｆ搴︽秷澶遍棶棰樸€?

### 3.5 璁粌锛氭搴﹁鍓?

```python title="chapters/sequence_models/basic_rnn/main.py" showLineNumbers
for epoch in range(5):
    for y in dataloader:
        y = y.to(device)
        hat_y = model(y)
        # ...
        loss = citerion(hat_y, label_y)
        optimizer.zero_grad()
        loss.backward()
        # key-start
        torch.nn.utils.clip_grad_norm_(model.parameters(), 0.5)  # 姊害瑁佸壀
        # key-end
        optimizer.step()
```

璁粌銆佽瘎浼板拰閲囨牱閫氳繃鏄惧紡 CLI 妯″紡閫夋嫨锛岄粯璁よ嚜鍔ㄤ娇鐢ㄥ彲鐢ㄧ殑 CUDA 鎴?CPU锛?

```bash
uv run python chapters/sequence_models/basic_rnn/main.py --model rnn1 --mode train
uv run python chapters/sequence_models/basic_rnn/main.py --model rnn1 --mode evaluate
uv run python chapters/sequence_models/basic_rnn/main.py --model rnn1 --mode sample
```

---

## 4. RNN1 vs RNN2 瀵规瘮

| 鐗规€?| RNN1 | RNN2 |
|------|------|------|
| 寰幆鍗曞厓 | 鎵嬪姩瀹炵幇锛?\tanh$ + Linear锛?| `nn.GRU` |
| 杈撳叆缂栫爜 | One-hot | `nn.Embedding` |
| 闅愯棌鍗曞厓鏁?| 32 | 64 |
| Dropout | 鉂?| 鉁?|
| 璁粌閫熷害 | 杈冩參锛堟墜鍔ㄥ惊鐜級 | 杈冨揩锛圙RU 浼樺寲锛?|

---

## 5. 寤朵几闃呰

- 馃搫 [Learning Representations by Back-propagating Errors锛圧umelhart et al.锛?986锛塢(https://www.nature.com/articles/323533a0)
- 馃搫 [Empirical Evaluation of Gated Recurrent Neural Networks on Sequence Modeling锛圕hung et al.锛?014锛塢(https://arxiv.org/abs/1412.3555) 鈥?GRU
- 馃敆 [鏈珷婧愮爜锛歚chapters/sequence_models/basic_rnn/`](https://github.com/luochang212/DL-Demos/tree/master/chapters/sequence_models/basic_rnn)
- 鉃★笍 **涓嬩竴绔?*锛歔鎯呮劅鍒嗘瀽 鈥?RNN 瀹炴垬](./sentiment-analysis)
